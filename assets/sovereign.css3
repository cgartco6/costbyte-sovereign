:root {
    --navy: #0a1f3d;
    --gold: #d4af37;
    --accent: #ffd700;
    --text: #e0e0e0;
    --bg: #0a1f3d;
}

* { margin:0; padding:0; box-sizing:border-box; }

body {
    background: var(--bg);
    color: var(--text);
    font-family: system-ui, sans-serif;
}

header nav {
    background: rgba(10,31,61,0.98);
    padding: 1rem 5%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    position: sticky;
    top: 0;
    z-index: 1000;
    border-bottom: 2px solid var(--gold);
}

.logo {
    font-size: 1.9rem;
    font-weight: 900;
    letter-spacing: 3px;
}
.logo span { color: var(--gold); }

/* Hamburger */
.hamburger {
    display: none;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 10px;
}

.hamburger span {
    width: 30px;
    height: 3px;
    background: var(--gold);
    transition: all 0.3s;
}

/* Navigation Menu */
#nav-menu {
    display: flex;
    gap: 2rem;
    list-style: none;
    align-items: center;
}

#nav-menu a {
    color: inherit;
    text-decoration: none;
}

#nav-menu a:hover { color: var(--gold); }

/* Hero */
.hero { position: relative; height: 70vh; overflow: hidden; }
.hero-image { width: 100%; height: 100%; object-fit: cover; filter: brightness(0.7); }

.hero-overlay {
    position: absolute;
    top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    color: white;
    text-shadow: 0 4px 20px rgba(0,0,0,0.8);
}

.urgency {
    background: #ff0000;
    color: white;
    padding: 8px 20px;
    display: inline-block;
    font-weight: bold;
    animation: flash 1.5s infinite;
    border-radius: 50px;
    margin-bottom: 1rem;
}

.cta-btn {
    background: var(--gold);
    color: #0a1f3d;
    border: none;
    padding: 1.2rem 3rem;
    font-size: 1.25rem;
    font-weight: bold;
    cursor: pointer;
    border-radius: 8px;
    transition: all 0.3s;
}

.cta-btn:hover {
    background: #fff;
    transform: scale(1.05);
}

.impulse-bar {
    background: var(--navy);
    color: var(--gold);
    text-align: center;
    padding: 1rem;
    font-weight: bold;
    border-bottom: 3px solid var(--gold);
}

/* Tiers */
.marketplace { padding: 5%; max-width: 1400px; margin: 0 auto; }

.grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 2rem;
}

.item-card {
    background: rgba(255,255,255,0.05);
    border: 1px solid var(--gold);
    padding: 2rem;
    text-align: center;
    transition: 0.3s;
    border-radius: 8px;
}

.item-card:hover { transform: translateY(-12px); }

.price { font-size: 2rem; color: var(--gold); margin: 1.5rem 0; font-weight: bold; }

/* Cart Modal */
.modal {
    display: none;
    position: fixed;
    top: 0; left: 0;
    width: 100%; height: 100%;
    background: rgba(0,0,0,0.85);
    z-index: 2000;
    align-items: center;
    justify-content: center;
}

.modal-content {
    background: var(--navy);
    padding: 2rem;
    max-width: 500px;
    width: 90%;
    border: 3px solid var(--gold);
    border-radius: 8px;
}

.total { font-size: 1.5rem; margin: 1.5rem 0; color: var(--gold); }

/* Animations */
@keyframes flash { 50% { opacity: 0.4; } }

/* RESPONSIVE HAMBURGER - FIXED VERSION */
@media (max-width: 768px) {
    .hamburger { display: flex; }

    #nav-menu {
        display: none;
        flex-direction: column;
        position: absolute;
        top: 100%;
        left: 0;
        width: 100%;
        background: var(--navy);
        padding: 1.5rem 5%;
        border-top: 2px solid var(--gold);
        box-shadow: 0 4px 10px rgba(0,0,0,0.5);
    }

    #nav-menu.active {
        display: flex;
    }
}
